import React from 'react';

const NotFoundPage = (props) => {

    return (
        <div className='categories__container'>
            404 - page not found! <a href="/">go to myLocations!</a>
        </div>
    );
};

export default NotFoundPage;